//helpers

// const crypto = require('crypto');

// const rawId = crypto.randomBytes(20).toString('hex');

// const id  = rawId.slice(4,15) + Date.now().toString + rawId.slice(12) + User.fullname.slice(0,2)
